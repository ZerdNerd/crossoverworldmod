scripted_gui = {
   dragon_counter = {
       window_name = "dragon_counter_container"
 
       context_type = player_context
 
       visible = {
            AND = {
            	tag = GRN
            	has_country_flag = dragon_immigration
            }
        }
    }

    railway_blocker = {
        window_name = "railway_has_to_be_blocked"

        parent_window_token = construction_tab

        context_type = player_context

        visible = {
            always = no
        #    ROOT = {
        #        NOT = {
        #            has_idea = modern_tech_idea
        #        }
        #    }
        }
    }
}